require "test_helper"

class SessionsControllerTest < ActionDispatch::IntegrationTest
  # test "the truth" do
  #   assert true
  # end
  test "create a new session when there is no user_id present"
  test "update an existing session with metadata from car data submissions"
end
